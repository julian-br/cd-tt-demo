---
// Der JavaScript-Teil bleibt genau gleich
export interface Props {
  title: string;
  humSrc: string;
  instrumentSrc: string;
  description?: string;
}

const { title, humSrc, instrumentSrc, description } = Astro.props;
---

<div class="bg-white border border-slate-200 rounded-xl p-6 shadow-md mb-8">
  <h3 class="text-xl font-semibold text-slate-800 mb-1">{title}</h3>
  {description && <p class="text-slate-600 mb-4">{description}</p>}

  <div class="flex flex-col sm:flex-row gap-6 mt-4">
    <div class="flex-1">
      <strong class="block mb-2 text-sm font-medium text-slate-700"
        >Input (Gesummt) 🎤</strong
      >
      <audio controls class="w-full" src={humSrc}></audio>
    </div>
    <div class="flex-1">
      <strong class="block mb-2 text-sm font-medium text-slate-700"
        >Output (Instrument) 🎻</strong
      >
      <audio controls class="w-full" src={instrumentSrc}></audio>
    </div>
  </div>
</div>
